@{ Layout = "~/Views/Shared/_MainLayout.cshtml"; }
@model DXWebApplication1.Models.OutlookScheduler

@section Left {
    <div class="navcontainer">
        @Html.DevExpress().DateNavigator(DXWebApplication1.Controllers.SchedulerHelper.Settings).Bind(Model.Appointments, Model.Resources).GetHtml()
    </div>
}

@Html.Partial("SchedulerPartial", Model)